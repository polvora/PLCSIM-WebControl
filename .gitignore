# The prebuilt PlcsimAutoStart.exe IS committed so the repo download is ready to run.
# Rebuild it locally with scripts\build.ps1 after changing src\.
*.pdb

# Runtime / machine-specific files (never commit)
appconfig.txt
autostart.log
boot-state.txt
SAFEMODE

# Release packages
dist/

# Editor / OS noise
.vs/
*.user
Thumbs.db
desktop.ini
